name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.4.0
        with:
          lfs: true
          persist-credentials: false

      - name: Install and Build 🔧
        run: |
          rm -rf node_modules package-lock.json
          npm install
          PUBLIC_PATH=./ npm run build
          mkdir build
          cp -R dev1/client/build build/dev1
          cp -R dev2/client/build build/dev2
          cp -R dev3/client/build build/dev3
          cp -R dev4/client/build build/dev4

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.8
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: gh-pages
          folder: build
          clean: true

      - name: Slack Notification
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_DEPLOYMENTS }}
